from django.apps import AppConfig


class AdorationConfig(AppConfig):
    """Django app configuration for the adoration application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "adoration"
